#include <stdio.h>

/**
 * mod - get remainder of the division of 2 numbers
 * @a: first int
 * @b: second int
 * Return: remainder of division of the numbers
 */
int mod(int a, int b)
{
	return (a % b);
}
